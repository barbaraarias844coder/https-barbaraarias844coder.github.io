version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # Optionally ignore example:
    # ignore:
    #   - dependency-name: "left-pad"
    #     versions:
    #       - "4.x"
